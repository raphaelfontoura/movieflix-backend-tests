import React from 'react';
import styles from './MovieDetail.module.css';

const MovieDetail = () => (
  <div className={styles.MovieDetail} data-testid="MovieDetail">
    MovieDetail Component
  </div>
);

export default MovieDetail;
